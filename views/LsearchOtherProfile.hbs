<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search/View Students Profile - Students</title>
    <link rel="stylesheet" href="../css/styles.css">
</head>
    <body>
        <!-- Header section containing the title and navigation menu -->
        <header>
            <img src="../images/logo.png" id="logo">
            <h1>View Profile</h1>
            <nav>
                <ul>
                    <li><a href="/labtechView/LSubProfile">Back</a></li>
                    <li><a href="/labtechPage">Home</a></li>
                </ul>
            </nav>
        </header>
        <main>
            <div class="section-container">
                <section class="search-form">
                    <h2>Search for User</h2>
                    <form action="/findUser2" method="POST">
                        <!-- User Search -->
                        Enter Name to Find User:
                        <input type="text" name="userName" required>
                        <button type="submit">Find User</button>
                    </form>
                </section>
                {{#if users}}
                    <section class="user-profiles">
                        <h2>User Profiles</h2>
                        {{#each users}}
                            <div class="user-card">
                                <p><strong>First Name:</strong> {{this.firstName}}</p>
                                <p><strong>Last Name:</strong> {{this.lastName}}</p>
                                <p><strong>Email:</strong> {{this.email}}</p>
                                <p><strong>Role:</strong> {{this.role}}</p>
                                <img src="../images/{{this.image}}" alt="User Image" height="250px" width="300px">
                            </div>
                        {{/each}}
                    </section>
                {{/if}}
            </div>
        </main>
        <!-- Footer section with copyright information -->
        <footer>
            <p>&copy; 2024 LabLink. All rights reserved.</p>
        </footer>
    </body>
</html>
